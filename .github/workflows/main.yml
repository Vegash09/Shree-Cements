name: Declarative Automation Bundles

on:
  push:
    branches:
      - dev
      - prod
  workflow_dispatch:
    inputs:
      target:
        description: "Deployment target"
        required: true
        default: dev
        type: choice
        options:
          - dev
          - prod

permissions:
  contents: read

# ══════════════════════════════════════════════════════════════
# JOB 1 – Validate
# ══════════════════════════════════════════════════════════════
jobs:
  validate:
    name: Validate declarative automation bundles
    runs-on: ubuntu-latest
    continue-on-error: false
    env:
      DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID }}
      DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET }}
      DATABRICKS_HOST: https://adb-7405607349730837.17.azuredatabricks.net/
      TARGET: ${{ github.event.inputs.target || github.ref_name }}  # 👈 no extra job needed
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Setup Databricks CLI
        uses: databricks/setup-cli@main

      - name: Ensure .py files are Databricks notebooks
        shell: pwsh
        run: |
          Get-ChildItem -Path "${{ github.workspace }}" -Filter "*.py" -Recurse | ForEach-Object {
            ("# Databricks notebook source`n" + (Get-Content $_.FullName -Raw)) | Set-Content $_.FullName
          }

      - name: Create databricks.yml
        run: |
          cat <<'EOF' > databricks.yml
          bundle:
            name: pharma_dabs

          include:
            - dbx/*.yml
            - dbx/*/*.yml

          sync:
            exclude:
              - ".github"
              - "databricks.yml"

          targets:
            dev:
              mode: development
              default: true
              workspace:
                host: https://adb-7405607349730837.17.azuredatabricks.net/
                root_path: ~/DABS/
              permissions:
                - user_name: sce21cs004@sairamtap.edu.in
                  level: CAN_MANAGE
                - service_principal_name: ${{ secrets.DATABRICKS_CLIENT_ID }}
                  level: CAN_MANAGE
            prod:
              mode: production
              workspace:
                host: https://adb-7405607349730837.17.azuredatabricks.net/
                root_path: /DABS/
              permissions:
                - user_name: sce21cs004@sairamtap.edu.in
                  level: CAN_MANAGE
                - service_principal_name: ${{ secrets.DATABRICKS_CLIENT_ID }}
                  level: CAN_MANAGE
          EOF

      - name: Validate bundle
        run: databricks bundle validate -t ${{ env.TARGET }}

# ══════════════════════════════════════════════════════════════
# JOB 2 – Deploy
# ══════════════════════════════════════════════════════════════
  deploy:
    name: Deploy declarative automation bundles
    needs: validate
    if: ${{ needs.validate.result == 'success' }}
    runs-on: ubuntu-latest
    continue-on-error: false
    env:
      DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID }}
      DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET }}
      DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      TARGET: ${{ github.event.inputs.target || github.ref_name }}  # 👈 same expression
    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Setup Databricks CLI
        uses: databricks/setup-cli@main

      - name: Ensure .py files are Databricks notebooks
        shell: pwsh
        run: |
          Get-ChildItem -Path "${{ github.workspace }}" -Filter "*.py" -Recurse | ForEach-Object {
            ("# Databricks notebook source`n" + (Get-Content $_.FullName -Raw)) | Set-Content $_.FullName
          }

      - name: Create databricks.yml
        run: |
          cat <<'EOF' > databricks.yml
          bundle:
            name: pharma_dabs

          include:
            - dbx/*.yml
            - dbx/*/*.yml

          sync:
            exclude:
              - ".github"
              - "databricks.yml"

          targets:
            dev:
              mode: development
              default: true
              workspace:
                host: https://adb-7405607349730837.17.azuredatabricks.net/
                root_path: ~/DABS/
              permissions:
                - user_name: sce21cs004@sairamtap.edu.in
                  level: CAN_MANAGE
                - service_principal_name: ${{ secrets.DATABRICKS_CLIENT_ID }}
                  level: CAN_MANAGE
            prod:
              mode: production
              workspace:
                host: https://adb-7405607349730837.17.azuredatabricks.net/
                root_path: /DABS/
              permissions:
                - user_name: sce21cs004@sairamtap.edu.in
                  level: CAN_MANAGE
                - service_principal_name: ${{ secrets.DATABRICKS_CLIENT_ID }}
                  level: CAN_MANAGE
          EOF

      - name: Deploy bundle
        run: databricks bundle deploy -t ${{ env.TARGET }}
